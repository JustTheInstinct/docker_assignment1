version: '1.0'

services:
  db:
    image: mysql:5.7
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: P@ssw0rd
      MYSQL_DATABASE: db
      MYSQL_USER: user
      MYSQL_PASSWORD: P@ssw0rd
    ports:
      - '3306:3306'
    expose:
      - '3306'

